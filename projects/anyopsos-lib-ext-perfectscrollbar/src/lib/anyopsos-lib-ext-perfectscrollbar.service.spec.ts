import { TestBed } from '@angular/core/testing';

import { AnyOpsOSLibExtPerfectscrollbarService } from './anyopsos-lib-ext-perfectscrollbar.service';

describe('AnyOpsOSLibExtPerfectscrollbarService', () => {
  beforeEach(() => TestBed.configureTestingModule({}));

  it('should be created', () => {
    const service: AnyOpsOSLibExtPerfectscrollbarService = TestBed.get(AnyOpsOSLibExtPerfectscrollbarService);
    expect(service).toBeTruthy();
  });
});
