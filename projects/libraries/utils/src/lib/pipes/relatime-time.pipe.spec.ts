import { RelatimeTimePipe } from './relatime-time.pipe';

describe('RelatimeTimePipe', () => {
  it('create an instance', () => {
    const pipe = new RelatimeTimePipe();
    expect(pipe).toBeTruthy();
  });
});
