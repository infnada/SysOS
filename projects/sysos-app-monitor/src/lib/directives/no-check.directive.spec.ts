import { NoCheckDirective } from './no-check.directive';

describe('NoCheckDirective', () => {
  it('should create an instance', () => {
    const directive = new NoCheckDirective();
    expect(directive).toBeTruthy();
  });
});
